<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BoardMapper">
	<select id="selectBoardList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT * FROM TBL_BOARD B 
		INNER JOIN TBL_USER U ON B.USERID = U.USERID 
		WHERE 1=1
		<if test="cateNum !='' and cateNum != null">
			AND CATEGORY = #{cateNum}
		</if>
		<if test="searchOption == ''">
			AND (TITLE LIKE '%' || #{keyword} || '%' 
			OR USERNAME LIKE '%' || #{keyword} || '%')
		</if>
		<if test="searchOption == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="searchOption == 'userName'">
			AND USERNAME LIKE '%' || #{keyword} || '%'
		</if>
		ORDER BY CDATETIME DESC  
		OFFSET #{startIndex} ROWS FETCH FIRST #{outputNumber} ROWS ONLY 
		
		
		<!--<choose>
			<when test="searchOption == 'title'">
				TITLE LIKE '%' || #{keyword} || '%'
			</when>
			<when test="searchOption == 'userName'">
				USERNAME LIKE '%' || #{keyword} || '%'
			</when>
			<otherwise>
				TITLE LIKE '%' || #{keyword} || '%' 
				OR USERNAME LIKE '%' || #{keyword} || '%'
			</otherwise>
		</choose>!-->
	</select>
	
	<delete id="deleteBoard" parameterType="hashmap">
		DELETE FROM TBL_BOARD 
		WHERE BOARDNO = #{boardNo} 
	</delete>
	
	<insert id="insertBoard" parameterType="hashmap" useGeneratedKeys="true" keyProperty="boardNo" keyColumn="boardNo">
		INSERT INTO 
		TBL_BOARD(BOARDNO, TITLE, CONTENTS, USERID, HIT, CDATETIME, UDATETIME) 
		VALUES(BOARD_SEQ.NEXTVAL,#{title},#{contents},#{sessionId},0,SYSDATE,SYSDATE)	
	</insert>
	<select id="viewBoard" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT * FROM TBL_BOARD 
		WHERE BOARDNO = #{boardNo}
	</select>
	
	<select id="countBoard" parameterType="hashmap" resultType="int">
		SELECT COUNT(*) CNT FROM TBL_BOARD B
		INNER JOIN TBL_USER U ON B.USERID = U.USERID 
		WHERE 1=1
		<if test="cateNum !='' and cateNum != null">
			AND CATEGORY = #{cateNum}
		</if>
		<if test="searchOption == ''">
			AND (TITLE LIKE '%' || #{keyword} || '%' 
			OR USERNAME LIKE '%' || #{keyword} || '%')
		</if>
		<if test="searchOption == 'title'">
			AND TITLE LIKE '%' || #{keyword} || '%'
		</if>
		<if test="searchOption == 'userName'">
			AND USERNAME LIKE '%' || #{keyword} || '%'
		</if>
	</select>
	<select id="boardComment" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT *FROM TBL_COMMENT C 
		INNER JOIN TBL_USER U ON C.USERID = U.USERID 
		WHERE BOARDNO = #{boardNo}
	</select>
	<insert id="insertFile" parameterType="hashmap" >	
		INSERT INTO 
		TBL_BOARDFILE  
		VALUES(BOARDFILE_SEQ.NEXTVAL,#{idx},#{filename},#{originFilename},#{path},#{size},#{fileext} ,'N')	
	</insert>
</mapper>