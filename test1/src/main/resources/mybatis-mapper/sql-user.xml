<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.UserMapper">
	<select id="userInfo" parameterType="hashmap" resultType="com.example.test1.model.User">
		SELECT * 
		FROM TBL_USER 
		WHERE USERID = #{userId}
	</select>
	
		
	<insert id="insertUser" parameterType="hashmap">
		INSERT INTO 
		TBL_USER VALUES(#{userId},#{pwd},#{userName},#{email},#{phone},#{gender})
	</insert>
	
	<select id="selectUserList" parameterType="hashmap" resultType="com.example.test1.model.User">
		SELECT * 
		FROM TBL_USER 
		WHERE USERID LIKE '%' || #{keyword} || '%'
	</select>
	
	<select id="seelctUserBoard" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT * 
		FROM TBL_BOARD
	</select>
	<delete id="userRemove" parameterType="hashmap">
		DELETE FROM TBL_USER 
		WHERE USERID = #{userId}
	</delete>
</mapper>